import React from "react";

class ErrorBoundary extends React.Component {
    constructor() {
        super(props);

        this.state = {
            hasError: false
        }
    }

    static changeStateOnError(error) {
        return t
    }
}